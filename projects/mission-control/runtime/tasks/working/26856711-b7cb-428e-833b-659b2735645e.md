# Working Memory — Token LinkedIn em plaintext

- Task ID: 26856711-b7cb-428e-833b-659b2735645e
- Status: done
- Risco: critical
- Responsável sugerido: main/flavia
- Origem: 2nd-brain/02-context/pending.md / 🔴 Críticas
- Criado em: 2026-04-26T11:02:33.928Z
- Atualizado em: 2026-05-01T12:36:00Z

## Objetivo

Remover token LinkedIn em plaintext do workspace e deixar resolução por referência segura.

## Critério de sucesso

Status rastreável com próximo passo claro; se exigir decisão externa, marcar como waiting_input.

## Resultado

Concluído em 2026-05-01.

- `config/linkedin-mensura.env` removido do workspace.
- Token migrado para fallback local root-owned `/root/.secrets/openclaw-linkedin.env` com permissão `0600`.
- `config/linkedin-mensura.json` passou a usar `accessToken` por referência `env` + `keespace`.
- `projects/openclaw-memory/agents/mensura/scripts/linkedin_post.py` atualizado para `scripts/secret_config.py`.
- Validação: `python3 scripts/secret_config.py config/linkedin-mensura.json --check` retornou `ok: true` e `plaintext_secret_paths: []`.

## Próximo passo

Para KeeSpace puro, desbloquear runtime do cofre com `KEESPACE_DATABASE` + `KEESPACE_PASSWORD_FILE` ou `KEESPACE_KEY_FILE` e rodar `OPENCLAW_SECRET_MODE=keespace python3 scripts/keespace_health.py --require-keespace`.
