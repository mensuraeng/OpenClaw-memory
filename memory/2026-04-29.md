
## Mission Control `/openclaw` operacional no domínio existente

- Alê pediu para substituir o plano de `openclaw.mensuraengenharia.com.br` e deixar operacional no domínio já existente `mc.mensuraengenharia.com.br`.
- Verificação DNS: `mc.mensuraengenharia.com.br` resolve; `mc.mensuranegenharia.com.br` não resolve, tratado como typo.
- Verificação externa: `https://mc.mensuraengenharia.com.br/openclaw` responde via Cloudflare/Nginx com `HTTP 307` para `/login?from=%2Fopenclaw`, confirmando rota protegida por login.
- PM2: `mission-control` reiniciado para carregar a build nova; status `online` na porta local `3001`.
- Verificação local: `http://127.0.0.1:3001/openclaw` responde com `HTTP 307` para login.
- Houve `HTTP 502` transitório imediatamente durante o restart, resolvido segundos depois com novo teste externo retornando `HTTP 307`.
