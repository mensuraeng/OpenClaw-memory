## APPEND 2026-04-14 06:32 — flush pré-compactação

### Lições aprendidas
- Em heartbeat/cron, quando a regra manda silêncio por padrão, não vale transformar lembrete interno, status de rotina ou contexto antigo em mensagem ao usuário.
- Em flush restrito desta sessão, a memória durável deve ir somente para `memory/2026-04-14.md`; tentar promover diretamente para `memory/context/*` conflita com a restrição operacional do momento.
- Atualização de workflow/cron ou backup local só merece interrupção se houver falha real de operação, risco ou decisão necessária; sucesso rotineiro deve ficar em memória, não em chat.

### Decisões do Alê
- Em checks de `HEARTBEAT.md`, usar exclusivamente `/root/.openclaw/workspace/HEARTBEAT.md`, sem recorrer a `docs/heartbeat.md` e sem reciclar tarefas antigas.
- Se nada exigir atenção no heartbeat, a saída correta é exatamente `HEARTBEAT_OK`.
- No flush pré-compactação desta sessão, persistir memórias duráveis apenas em `memory/2026-04-14.md`, tratando arquivos-base como somente leitura.

### Pendências
- Executar o health check operacional das 06:00 e só avisar o usuário se surgir falha real de operação.
- Revisar por que houve resposta indevida ao usuário após lembrete interno de backup/rotina, mantendo aderência estrita à política de silêncio do heartbeat.
- Continuar pendente a definição da política do WhatsApp `miafinance` para evitar descarte silencioso de mensagens de grupo.
- Continuam pendentes a revisão do papel do agente `autopilot` e a limpeza da config stale de `plugins.entries.device-pair`.

### Fatos duráveis do dia
- Durante a madrugada de 2026-04-14, os heartbeats foram reiteradamente tratados sob a regra de silêncio, com leitura explícita de `/root/.openclaw/workspace/HEARTBEAT.md`.
- Houve um lembrete interno registrando falha de autenticação IMAP no job `Monitor Licitações PNCP`, indicando necessidade de revisão manual de credenciais se o erro persistir na próxima janela operacional.
- Foi registrado backup local do workspace com commit `d45beeb`, incluindo novos artefatos documentais e de runbook em `docs/` e `system/runs/2026-04-14/`.

