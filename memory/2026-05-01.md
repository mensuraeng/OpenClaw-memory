# Memória diária — 2026-05-01

## Pre-compaction memory flush — 2026-05-01 11:58 UTC

- Conversa recente com Alê definiu e implantou a Fase 1A do novo agente **Sentinela Operacional**.
- Objetivo do Sentinela: fiscal operacional do ecossistema OpenClaw / 2nd-brain / crons / agentes / integrações / banco / GitHub; opera por exceção, com evidência obrigatória, sem decisão estratégica e sem saída externa.
- Caminho canônico criado: `/root/2nd-brain/06-agents/sentinela/`.
- Fase 1A implantada sem cron ativo e sem UUID real:
  - `AGENT.md`
  - `AGENT-MAP.md`
  - `README.md`
  - `probes/self_pulse.sh`
  - `probes/check_secrets.sh`
  - `observer/observer.sh`
  - `templates/healthcheck.example.yml`
  - `templates/observer-healthcheck.example.yml`
  - `state/.gitkeep`
- Também foram atualizados:
  - `/root/2nd-brain/.gitignore`
  - `/root/2nd-brain/_system/AGENT-ACTIVATION-MATRIX.md`
- Validação declarada da Fase 1A:
  - scripts passaram em `bash -n`;
  - arquivos reais de Healthchecks, logs e JSONL runtime estão ignorados pelo Git;
  - `self_pulse.sh` falha corretamente sem config real;
  - `check_secrets.sh` rodou em modo heurístico e retornou S1 esperado para arquivos sensíveis existentes, mas ignorados.
- Commit/push do Sentinela Fase 1A no 2nd-brain: `4bbc92a agents: add sentinela phase 1a`.
- Criado lembrete/job para 2026-05-08 09:00 BRT: `sentinela-upgrade-fase-2-review`, objetivo revisar estabilidade e preparar Fase 2 (`check_git.sh`, `check_health.sh`, baseline).
- Durante implantação, backup automático inicial falhou por disco cheio. Foi removido apenas um backup `.tmp` incompleto de 7,2 GB; backups válidos foram mantidos.
- Alê perguntou sobre backup externo. Confirmado backup full externo da VPS no Backblaze B2:
  - bucket: `flavia-backups`;
  - prefixo último backup verificado: `flavia/vps-full/20260501T083001Z/`;
  - escopo: `/root`, `/etc`, `/home`, `/var`, `/opt`, `/usr/local`, `/srv`;
  - manifesto local: `runtime/backups/vps-full-manifests/vps-full-20260501T083001Z.manifest.json`;
  - health informado: `backblaze_b2=ok` e `vps_full_backup=ok`.
- Discussão de política de backup local: manter **2 versões locais válidas** em `/root/openclaw-backups`, externo B2 como retenção principal e manifestos locais como mapa de restauração.
- Limpeza/política concluída:
  - mantidas 2 versões locais válidas em `/root/openclaw-backups`;
  - removidos tarballs antigos/quebrados;
  - espaço livre subiu de 7,5 GB para 16 GB;
  - uso do disco caiu de 93% para 84%.
- Política ajustada:
  - `scripts/backup_retention_b2.py`: `LOCAL_KEEP` ajustado para 2;
  - `scripts/backup_before_change.sh`: após backup estrutural, limpa `.tmp/.partial` e mantém só 2 backups locais válidos mais recentes;
  - cron `vps-limpeza-diaria`: atualizado para manter 2 versões, não 3.
- Commit/push da política de backup local: `672045f chore: retain two local openclaw backups`.
- Próximo ajuste recomendado: Sentinela fiscalizar uso de disco acima de 85–90% e presença de `.tmp` preso.

## Backup externo — versão de rollback (2026-05-01)

- Alê autorizou manter uma versão de rollback na externa.
- Política aplicada em `scripts/backup_retention_b2.py`:
  - B2 passa a manter 3 conjuntos remotos válidos em `flavia/vps-full/` e `flavia/2nd-brain/`;
  - local continua mantendo 2 versões válidas para preservar disco da VPS.
- Dry-run após ajuste confirmou que os três conjuntos existentes no B2 ficam preservados e nada remoto seria apagado:
  - VPS full: `20260501T083001Z`, `20260430T083001Z`, `20260429T083001Z`;
  - 2nd-brain: `20260501T081022Z`, `20260430T081001Z`, `20260429T081001Z`.
- Decisão permanente registrada em `/root/2nd-brain/02-context/decisions.md`.
## Hostinger snapshot / MCP — pre-compaction flush 2026-05-01 16:05 UTC

- Alê quer estruturar snapshot diário da VPS Hostinger via API oficial com foco em segurança, rastreabilidade e simplicidade operacional.
- Objetivo da rotina diária: **1) backup do workspace no GitHub; 2) snapshot da VPS na Hostinger**.
- Premissa validada com Alê: snapshot Hostinger sobrescreve versão anterior e não serve como histórico; portanto backup GitHub/versionado antes do snapshot é obrigatório para reduzir risco de sobrescrever estado bom por estado bugado.
- VPS ID informado por Alê: `1546384`. Foi tratado como `HOSTINGER_VM_ID`.
- Regras definidas pelo Alê:
  - explicar antes de cada ação o que será feito e por que importa;
  - pedir confirmação antes de qualquer mudança estrutural;
  - validar o que já estiver pronto e seguir;
  - no final entregar resumo seco do implantado, riscos e limitações.
- Diagnóstico read-only feito antes da compactação:
  - `/root/.openclaw/workspace/scripts/backup_workspace.sh` existe, faz commit/push do workspace, mas usa `git push origin HEAD || true`; para rotina de snapshot isso é risco porque pode falhar silenciosamente e ainda seguir para snapshot.
  - `/root/.openclaw/workspace/scripts/hostinger_snapshot_check.py` existe para consulta read-only de snapshot.
  - `/root/.openclaw/workspace/scripts/hostinger_snapshot_create.py` existe para criar snapshot manual com `--yes`; não restaura/apaga.
  - `/root/.openclaw/workspace/skills/hostinger-vps-snapshot/SKILL.md` existe e orienta consulta/criação segura.
  - `/root/2nd-brain/03-knowledge/tutorial-configurar-hostinger-mcp-openclaw.md` existe com tutorial MCP Hostinger/OpenClaw.
  - `/root/2nd-brain/03-knowledge/hostinger-vps-snapshots.md` existe com política: Hostinger snapshot = rollback rápido; B2/GitHub = histórico/versionamento.
- Ainda pendente:
  - configurar `HOSTINGER_API_TOKEN` em arquivo protegido;
  - confirmar que `HOSTINGER_VM_ID=1546384` está gravado no ambiente seguro;
  - listar VPS via API e validar que `1546384` é a máquina correta;
  - confirmar por API/docs se Hostinger não oferece download/export de snapshot;
  - criar script shell principal pedido: `/root/.openclaw/workspace/scripts/hostinger-vps-snapshot.sh`;
  - criar wrapper diário que execute backup GitHub com falha explícita antes de snapshot;
  - criar/ativar cron diário somente depois de teste read-only e teste manual de snapshot;
  - validar permissões do arquivo de credencial e ausência de token em logs/git/config pública.
- Cron anterior citado: `hostinger-snapshot-check-daily` (`2454836f-b252-44fb-a733-210df7c7b453`) era read-only/inativo para checagem, não o cron final de snapshot diário.
- Alê perguntou se é possível fazer fallback/compatibilidade da versão `.24` sem perder contexto. Resposta dada: sim, usando Node 24 isolado para o MCP Hostinger via `nvm`/wrapper, sem trocar Node global do OpenClaw; trocar Node global é mais arriscado e pode afetar runtime/plugins.

## Hostinger MCP — Node 24 isolado implantado (2026-05-01)

- Antes da mudança estrutural foi criado backup verificado: `/root/openclaw-backups/2026-05-01T16-07-37.075Z-openclaw-backup.tar.gz`, label `hostinger-node24-wrapper`, `verified=true`.
- Node global do OpenClaw permaneceu em `v22.22.2`; não foi alterado.
- Node 24 isolado instalado em `/root/.openclaw/tools/node-v24.15.0-linux-x64`.
- MCP Hostinger instalado isolado em `/root/.openclaw/mcp/hostinger-api-mcp` com pacote `hostinger-api-mcp@0.1.37`.
- Wrapper MCP atualizado em `/root/.openclaw/workspace/scripts/mcp/hostinger-api-mcp-wrapper.sh` para usar Node 24 isolado, carregar `/root/.openclaw/.env`, exigir `HOSTINGER_API_TOKEN` e executar o binário isolado.
- Validações realizadas:
  - `bash -n scripts/mcp/hostinger-api-mcp-wrapper.sh` ok;
  - Node 24 responde `v24.15.0`;
  - MCP responde `--help` via binário isolado;
  - wrapper responde `--help` com token dummy sem rede;
  - wrapper falha com `exit_code=2` quando `HOSTINGER_API_TOKEN` não está configurado.
- Estado pendente: `HOSTINGER_VM_ID` existe em `/root/.openclaw/.env`, mas `HOSTINGER_API_TOKEN` ainda não existe; autenticação real/API Hostinger não foi testada.
- MCP Hostinger registrado no OpenClaw config com `openclaw mcp set hostinger-api` apontando para `/root/.openclaw/workspace/scripts/mcp/hostinger-api-mcp-wrapper.sh`.
- Gateway recebeu restart/SIGUSR1 para carregar config do MCP.
- Commits/push:
  - workspace: `83fa220 feat: add hostinger mcp snapshot tooling`;
  - 2nd-brain: `837fad6 docs: document hostinger mcp node24 setup`.

## Rollback OpenClaw 2026.4.24 — evidência pós-validação (2026-05-01)

- Alê pediu rollback para o estado do backup de hoje com OpenClaw `2026.4.24` e exigiu ficar 100%.
- Backup alvo local inicialmente inspecionado: `/root/openclaw-backups/2026-05-01T12-22-53.753Z-openclaw-backup.tar.gz`; manifest/config indicavam `lastTouchedVersion=2026.4.24`.
- Erro operacional: ao criar backup de resgate com `backup_before_change.sh`, a retenção local manteve só os 2 backups mais recentes e removeu o backup local das 09:22 BRT. Backup externo B2/VPS full continua sendo a proteção histórica.
- Backup de resgate criado antes do rollback: `/root/openclaw-backups/2026-05-01T16-53-28.143Z-openclaw-backup.tar.gz`, label `pre-openclaw-2026-4-24-rollback`, `verified=true`.
- Rollback aplicado:
  - pacote global `openclaw@2026.4.24` instalado;
  - CLI voltou para `OpenClaw 2026.4.24 (cbcfdf6)`;
  - package version `/usr/lib/node_modules/openclaw/package.json` = `2026.4.24`;
  - MCP Hostinger removido do config;
  - Node global permaneceu `v22.22.2`.
- Problema pós-rollback: runtime deps da versão `2026.4.24` ficaram incompletos. Corrigidas dependências ausentes no runtime `/root/.openclaw/plugin-runtime-deps/openclaw-2026.4.24-4eca5026e977`:
  - `tslog@4.10.2`;
  - `json5@2.2.3`;
  - `jszip@3.10.1`;
  - `tar@7.5.13`;
  - `@vincentkoc/qrcode-tui@0.2.1`;
  - `sqlite-vec@0.1.9`.
- Validação final:
  - `openclaw status --json` retornou código 0 e JSON válido;
  - `python3 scripts/operational_health.py` retornou `overall=active`, 11 OK e 1 pending esperado (`linkedin_institutional_community_api=no_approval_found`);
  - tail recente do gateway sem `Cannot find package`, sem `errorCode=UNAVAILABLE` e sem `channel exited` nos últimos registros inspecionados.
