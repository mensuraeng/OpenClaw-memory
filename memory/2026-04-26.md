# Memória — 2026-04-26

## Projeto Trade — criação e escopo
- Criado novo projeto pessoal **Trade**, separado de Mensura/MIA/PCS, para construir sistema de monitoramento de mercado, pesquisa, Strategy Lab, paper/shadow trading, Risk Gate, journal e relatórios.
- Guardrails centrais definidos: governança antes de automação; sem dinheiro real no MVP; sem execução autônoma; sem cripto, alavancagem, opções, short, margem ou day trade real no início; qualquer execução real futura exige confirmação humana.

<!-- compactado em 2026-04-26 21:50:01 BRT: 536 -> 200 linhas -->

## CLI Anything — smoke test 10/10 e critério de deploy
- Rodado smoke test das 10 CLIs implantadas/expandidas.
- Todas responderam status/consulta básica com OK: `openclaw-ops`, `msgraph`, `finance`, `mensura-schedule`, `commercial`, `pcs-licita`, `mia-obra`, `knowledge`, `sienge`, `reports`.
- Guardrails bloquearam corretamente comandos perigosos: `restart`, `send`, `pix`, `publish`, `email`, `sync`, `upload` e `send` em reports.
- Relatório criado em `/root/.openclaw/workspace/docs/CLI-SMOKE-TEST-2026-04-26.md`.
- Decisão operacional recomendada: CLI é melhor que scripts soltos porque padroniza interface, teste, saída estruturada e guardrails. Deploy deve ser faseado: primeiro crons read-only/diagnósticos; fluxos com envio externo/upload/pagamento/agenda/delete/update/restart continuam bloqueados até autorização e refatoração.

## Sienge — CLI liberada sob ordem explícita do Alê
- Por orientação do Alê, a CLI `sienge` deixou de ser somente bloqueio absoluto e passou para modo `controlled_v0.2`.
- Upload real agora é tecnicamente permitido apenas via subcomando controlado: `sienge budget upload-authorized <arquivo.xlsx> --execute --approved-by Alexandre --confirm "AUTORIZO SIENGE" --approval-note "..."`.
- Sem `--execute`, a CLI roda dry-run por padrão.
- Sem confirmação exata `AUTORIZO SIENGE`, aprovador Alexandre/Alê e justificativa, o upload real é bloqueado.
- A CLI não envia mensagem externa automaticamente, não deleta, não faz rollback automático e não expõe credenciais.
- Validação executada: py_compile OK; status OK; guardrails OK; validate de Excel OK; bloqueio por confirmação errada OK; dry-run genérico com Excel de teste OK; comando top-level `send` bloqueado OK.

## CLI Anything — MIA/PCS/Knowledge liberados sob ordem explícita
- Por decisão do Alê, `mia-obra`, `pcs-licita` e `knowledge` foram alterados de bloqueio absoluto para modo `controlled_v0.2`, seguindo o padrão do Sienge.
- `pcs-licita` agora permite relatório/email semanal real apenas via `weekly send-authorized --execute --approved-by Alexandre --confirm "AUTORIZO PCS LICITA" --approval-note "..."`.
- `mia-obra` agora permite rotinas internas CCSP reais apenas via subcomandos autorizados: `ccsp manha-send-authorized`, `ccsp rdo-send-authorized`, `ccsp weekly-send-authorized`, todos exigindo `--execute`, aprovador, confirmação exata `AUTORIZO MIA OBRA` e justificativa.
- `knowledge` agora permite sync real de Notion/NotebookLM/sync_knowledge apenas via `sync-authorized`/`notion sync-authorized`/`notebooklm sync-authorized` com `--execute`, aprovador, confirmação exata `AUTORIZO KNOWLEDGE` e justificativa.
- Validação: py_compile OK nas três CLIs; status/guardrails OK; confirmação errada bloqueou corretamente; previews/dry-runs não executaram mutation crítica.
- Observação operacional: dry-run de `pcs-licita weekly send-authorized` gerou alerta interno indicando 13 licitações urgentes, com 5 vencendo amanhã cedo. Recomendada triagem PCS na manhã de 2026-04-27.

## CLI Anything — MIA/PCS/Knowledge automáticos
- O Alê corrigiu a regra: para `mia-obra`, `pcs-licita` e `knowledge`, não exigir confirmação manual por comando; esses fluxos são controláveis internamente e podem operar automaticamente quando acionados pelo subcomando adequado.
- Ajuste aplicado: as três CLIs passaram para `automatic_v0.3`.
- `pcs-licita weekly send-authorized --execute` pode enviar o relatório semanal aprovado sem frase de autorização; mantém bloqueio contra Telegram direto/delete/remove.
- `mia-obra ccsp manha-send-authorized --execute`, `rdo-send-authorized --execute` e `weekly-send-authorized --execute` podem executar as rotinas internas aprovadas; mantém guardrail CCSP e bloqueio contra Telegram direto/delete/remove.
- `knowledge notion sync-authorized --execute`, `notebooklm sync-authorized --execute` e `knowledge sync-authorized --execute` podem rodar sync aprovado; mantém bloqueio contra delete/upload/send e regra de não versionar segredos.
- Validação: py_compile OK; guardrails atualizados OK; help dos subcomandos confirma que não há mais flags de aprovação manual.

## CLI Anything — deploy operacional em crons
- Implantado deploy operacional inicial dos crons para usar CLIs agent-native.
- Knowledge:
  - `notion-sync-nightly` agora chama `knowledge notion sync-authorized --execute --timeout 900`.
  - `notebooklm-nightly-sync` agora chama `knowledge notebooklm sync-authorized --execute --timeout 900`.
- MIA Obra:
  - `cobranca-rdo` agora chama `mia-obra ccsp rdo-send-authorized --execute --timeout 600`.
  - criado `mia-ccsp-manha-victor-cli` às 08:00 seg-sex com `mia-obra ccsp manha-send-authorized --execute`.
  - criado `mia-ccsp-relatorio-semanal-cli` segunda 08:00 com `mia-obra ccsp weekly-send-authorized --execute`.
- PCS Licita:
  - criado `pcs-licitacoes-email-semanal-cli` segunda 09:00 com `pcs-licita weekly send-authorized --execute`.
  - `pcs-prazos-diario` principal agora usa `pcs-licita search --dias 30 --valores --timeout 300`.
  - cron legado duplicado `pcs-prazos-diario` 06:30 foi desativado.
- OpenClaw/Ops:
  - `cost:guard-daily` agora usa `openclaw-ops cost daily --json`.
  - `Watchdog - Monitor de Crons` agora usa `openclaw-ops health --json` e `openclaw-ops backup status --json`.
  - criado `openclaw-ops-backup-status-cli` às 06:25 seg-sáb.
  - cron duplicado `finance-custo-tokens` diário 20h foi desativado.
- Finance:
  - `finance-fluxo-semanal` agora usa `finance status/report/validate-contas`.
- Mensura Schedule:
  - criado `mensura-schedule-daily-quality-cli` às 07:20 seg-sex com `critical-summary`, `quality-report` e `audit-orphans`.
- MS Graph:
  - criado `msgraph-health-daily-cli` às 06:10 seg-sáb com `msgraph health --json`.
- Validação: todos os subcomandos usados nos crons responderam `--help`; execuções read-only de `openclaw-ops cost`, `openclaw-ops backup status`, `finance status`, `mensura-schedule quality-report` e `msgraph health` rodaram OK.
- Snapshot pós-deploy dos jobs salvo em `/root/openclaw-backups/manual/cron-jobs-after-cli-deploy-20260426-1916.json`.
- Complemento do deploy CLI em crons: criados também `commercial-weekly-readonly-cli`, `reports-weekly-preview-cli` e `sienge-weekly-status-cli` como diagnósticos read-only. Validação read-only OK para `commercial status/pipeline`, `reports status/list` e `sienge status`. Snapshot final: `/root/openclaw-backups/manual/cron-jobs-after-cli-deploy-final-20260426-1920.json`.

## CLI Anything — LinkedIn/comercial automáticos
- O Alê autorizou publicação LinkedIn/comercial automática.
- `commercial` CLI saiu do modo read-only para `automatic_v0.3`.
- Novo comando: `commercial linkedin publish-auto --execute --file <arquivo>` publica no LinkedIn pessoal do Alê usando `openclaw-linkedin`, sempre renderizando preview antes e bloqueando texto curto acidental.
- Novos comandos operacionais automáticos:
  - `commercial pipeline report --execute` executa `monitoramento_comercial_mensura.py` e envia relatório comercial Mensura.
  - `commercial marketing daily --execute` executa `operacional_marketing_mensura.py` e envia prompt operacional ao Marketing.
- Crons LinkedIn pessoal terça/quinta foram atualizados para publicar via CLI `commercial linkedin publish-auto --execute`, sem pedir aprovação manual.
- Cron `commercial-weekly-readonly-cli` foi convertido para `commercial-daily-report-cli` seg-sex 08:35 com `commercial pipeline report --execute`.
- Criado `commercial-marketing-daily-cli` seg-sex 09:10 com `commercial marketing daily --execute`.
- Guardrails mantidos: sem delete/remove/upload; páginas institucionais ainda dependem de mapear organization URN/admin antes de publicação por página.
- Validação: py_compile OK; `commercial linkedin check-config` OK; `publish-auto` dry-run OK; helps dos novos subcomandos OK. Não foi feito live post manual durante validação.
- Snapshot dos crons pós-ajuste: `/root/openclaw-backups/manual/cron-jobs-after-commercial-auto-20260426-1927.json`.

## CLI Anything — mapas finais dos agentes
- Após checagem, foi identificado que código/crons/memória estavam commitados, mas nem todos os `AGENT-MAP.md` tinham o bloco consolidado de CLI/commercial/knowledge/sienge.
- Criado backup antes da mudança: `/root/openclaw-backups/manual/agent-maps-before-cli-final-map-20260426-193021.tar.gz`.
- Atualizados todos os `AGENT-MAP.md` em `/root/2nd-brain/06-agents/*/` com a seção “Infra operacional compartilhada — CLI command layers (2026-04-26)”.
- A seção lista os 11 CLIs (`openclaw-ops`, `msgraph`, `finance`, `mensura-schedule`, `commercial`, `pcs-licita`, `mia-obra`, `knowledge`, `sienge`, `reports`, `trade`), decisões de automação, restrições do Sienge, isolamento do Trade, separação do Mensura Schedule Control e snapshots dos crons.

## Mensura Schedule Control — analytics executivos implantados
- Implantados novos comandos na CLI `mensura-schedule` para transformar a base importada em análise executiva:
  - `project-classify`: classifica projetos como válidos, modelo/auxiliar, duplicado provável ou revisão por baixa qualidade.
  - `baseline-discovery`: identifica sinais de baseline por versão/projeto.
  - `risk-report`: ranking executivo de risco usando críticas abertas/atrasadas, atraso vs baseline quando existir e qualidade de dados.
  - `populate-weekly-metrics`: popula/upserta `analytics.project_weekly_metrics` a partir das métricas atuais.
- Executado `populate-weekly-metrics --execute`: 37 projetos upsertados em `analytics.project_weekly_metrics`.
- Validação atual: `project_weekly_metrics_count = 37`.
- Resultado de análise inicial:
  - `baseline-discovery --only-candidates` retornou 0; ou seja, a base importada ainda não tem baseline explícita/candidata confiável. Próxima etapa: selecionar/registrar baseline aprovada por projeto.
  - `project-classify` classificou 33 projetos como `review_low_date_quality` e 4 como `exclude_model_or_auxiliary`; não há ainda projeto “valid_for_analytics” sem revisão porque datas atuais/finish estão incompletas em massa.
  - Top risco atual por críticas abertas: `GOLF_MASTER_SCHEDULE`, `OBRA_MODELO_MASTER_SCHEDULE`, `COLLECTION_PAULISTA`, `MIRANT_IBIRAPUERA`, `PEN_NSULA_MADALENA`.
- Cron `mensura-schedule-daily-quality-cli` atualizado para rodar `populate-weekly-metrics --execute`, `risk-report`, `project-classify`, `baseline-discovery --only-candidates` e `audit-orphans` diariamente.

## Mensura Schedule Control — data readiness implantado
- Implantado comando `mensura-schedule data-readiness` como semáforo oficial de aptidão por projeto para análise/predição.
- O comando calcula cobertura de datas atuais, percentual, crítica, baseline, datas invertidas, percentuais inválidos, score de readiness, nível e ação recomendada.
- Validação no Supabase cloud: 37 projetos analisados.
- Resultado atual:
  - 33 projetos em `BLOQUEADO_DATAS_ATUAIS`, com `current_finish_cov = 0.00` e baseline também 0.00 nos principais.
  - 4 projetos em `EXCLUIR_MODELO_AUXILIAR`.
  - Nenhum projeto pronto para predição ainda.
- Interpretação: o gargalo real agora está claro — o importador ou o mapeamento precisa capturar corretamente as datas atuais de término (`current_finish`) e depois registrar baseline aprovada.
- Cron `mensura-schedule-daily-quality-cli` atualizado para incluir `data-readiness` diariamente.

## Mensura Schedule Control — correção da coluna Término
- O Alê informou que o cronograma tem a coluna `Término`.
- Causa raiz confirmada: a coluna estava mapeada, mas os valores vinham do MS Project em português com prefixo de dia da semana, ex.: `Dom 02/08/26`, `Qui 11/09/25`; o parser aceitava `02/08/26`, mas não removia o prefixo, então datas viravam `NULL`.
- Corrigido parser de datas em `ingest_sharepoint_schedules.py` e adicionado comando `mensura-schedule repair-date-fields` para reparsear datas a partir de `raw_payload`.
- Backfill executado no Supabase cloud em lotes SQL: `current_finish` preenchido em 106.189/107.145 atividades; `baseline_finish` preenchido em 103.772/107.145; `current_start` em 106.189/107.145.
- Após recalcular `populate-weekly-metrics` e `data-readiness`:
  - 31 projetos ficaram `PRONTO_PREDITIVO`.
  - 2 projetos ficaram `PRONTO_ANALISE_CONTROLE`.
  - 4 seguem `EXCLUIR_MODELO_AUXILIAR`.
- O gargalo mudou: datas e baseline agora estão utilizáveis; próxima etapa é filtrar modelos/auxiliares e interpretar o `risk-report` com baseline x atual.

## Mensura Schedule Control — universo analítico e forecast inicial
- Implantados comandos:
  - `mensura-schedule analytics-universe --analytics-only`: separa projetos aptos do universo analítico, excluindo modelos/auxiliares/testes.
  - `mensura-schedule forecast-initial --execute`: gera forecast heurístico auditável em `analytics.forecasts` para término de projeto.
- Universo atual: 37 projetos totais; 33 entram no universo analítico; 4 excluídos como modelo/auxiliar/teste.
- Status do universo analítico: 31 `predictive`, 2 `control_only`.
- Executado `forecast-initial --execute`: inseridos 33 forecasts (`model_name=heuristic_schedule_delay`, `model_version=v0.1`, `forecast_type=project_finish`).
- Top forecasts por atraso esperado inicial: `GOLF_MASTER_SCHEDULE` 282 dias, `SOMA_GALERIA` 132 dias, `COLLECTION_MADALENA` 123 dias, `COLLECTION_PAULISTA` 109 dias, `COLLECTION_VL_CLEMENTINO` 109 dias, `SIDE_SACOM` 108 dias, `DF345_DIOGO_DE_FARIA` 95 dias, `FASANO_SCHEDULE` 76 dias.
- Cron `mensura-schedule-daily-quality-cli` atualizado para rodar `analytics-universe --analytics-only` e `forecast-initial --execute` diariamente junto com readiness/risk/audit.

## Mensura Schedule Control — relatório executivo de risco
- Implantado comando `mensura-schedule executive-risk-report`.
- O relatório filtra modelos/auxiliares, cruza métricas atuais com `analytics.forecasts`, e retorna risco executivo por projeto com ação recomendada.
- Campos principais: críticas abertas/atrasadas, avanço médio, baseline finish, current finish, delay vs baseline, forecast finish, atraso esperado, probabilidade de atraso, P80/P95 e recomendação.
- Validação: JSON e markdown OK.
- Top 5 atual do relatório executivo: `GOLF_MASTER_SCHEDULE`, `PEN_NSULA_MADALENA`, `COLLECTION_PAULISTA`, `SOMA_GALERIA`, `COLLECTION_VL_CLEMENTINO`.
- Cron `mensura-schedule-daily-quality-cli` atualizado para incluir `executive-risk-report --limit 10 --json`.

## Mensura Schedule Control — relatório apenas projetos em andamento
- O Alê definiu que relatórios/forecast devem considerar somente projetos em andamento; projetos com `current_finish` anterior à data atual devem ser excluídos.
- Ajustado `mensura-schedule executive-risk-report` para filtrar `max_current_finish >= current_date`.
- Ajustado `mensura-schedule forecast-initial` para gerar preview/insert apenas de projetos com término atual maior ou igual à data atual.
- Validação em 2026-04-26: relatório executivo passou de 33 para 8 projetos em andamento.
- Projetos em andamento no relatório atual: `MELICITA`, `DOPPIO`, `DF345_DIOGO_DE_FARIA`, `P_G`, `MELICITA_R1`, `ELEV_ALTO_DO_IPIRANGA`, `SOFITEL_DIRETOR`, `CCN_BIOMA`.

## Portfólio obras em andamento — Mensura/MIA/PCS
- Criado relatório cruzado: `/root/.openclaw/workspace/docs/cronogramas/PORTFOLIO-OBRAS-EM-ANDAMENTO-2026-04-26.md`.
- Critério: Mensura vem do Supabase Schedule Control (`current_finish >= current_date`); MIA/PCS vêm do 2nd-brain/Sienge/memória operacional quando não há cronograma importado.
- Mensura confirmados em andamento pelo banco: 8 projetos — `MELICITA`, `DOPPIO`, `DF345_DIOGO_DE_FARIA`, `P_G`, `MELICITA_R1`, `ELEV_ALTO_DO_IPIRANGA`, `SOFITEL_DIRETOR`, `CCN_BIOMA`.
- MIA confirmado em andamento: `CCSP Casa 7`, término 2026-05-22, ainda fora do Supabase Schedule Control.
- PCS: nenhum projeto confirmado por cronograma; candidatos/validar incluem `Teatro Município de Suzano`, `Paranapiacaba/Pavimentação Paranapiacaba` e `SPTrans — 1ª OS` quando recebida.
- Próximo passo recomendado: criar camada `portfolio.project_registry`, importar/registrar CCSP Casa 7, e classificar PCS/Sienge entre obra ativa, encerrada, administrativo e ambíguo.

## Mensura Schedule Control — exclusões operacionais do relatório executivo
- O Alê determinou desconsiderar do relatório executivo: `DOPPIO`, `MELICITA`, `ELEV_ALTO_DO_IPIRANGA`, `SOFITEL_DIRETOR`, `CCN_BIOMA`.
- Regra específica: `MELICITA` e `MELICITA_R1` são a mesma obra; manter `MELICITA_R1` como referência válida e excluir `MELICITA`.
- `executive-risk-report`, `forecast-initial` e `analytics-universe --analytics-only` foram ajustados para respeitar essas exclusões.
- Validação: `executive-risk-report` agora retorna 3 projetos Mensura em andamento: `DF345_DIOGO_DE_FARIA`, `P_G`, `MELICITA_R1`.

## Mensura Schedule Control — exclusão Diogo de Faria
- O Alê determinou também excluir `DF345_DIOGO_DE_FARIA` do relatório executivo.
- `executive-risk-report` validado após ajuste: agora retorna apenas `P_G` e `MELICITA_R1`.

## Portfolio Registry — cadastro mestre estrutural de projetos
- Criado schema `portfolio` no Supabase Mensura Schedule Control.
- Migration: `projects/mensura-schedule-control/supabase/migrations/20260427003000_portfolio_project_registry.sql`.
- Tabelas: `portfolio.project_registry`, `portfolio.project_aliases`.
- View: `portfolio.v_project_registry_current`.
- CLI adicionados: `mensura-schedule portfolio-seed --json` e `mensura-schedule portfolio-registry --json`.
- Seed inicial aplicado: 12 registros — 8 Mensura, 1 MIA, 3 PCS.
- Relatórios migrados para usar registry em vez de hardcode: `executive-risk-report`, `forecast-initial`, `analytics-universe --analytics-only`.
- Validação: universo analítico, forecast e relatório executivo retornam apenas `P_G` e `MELICITA_R1` para Mensura.
- Documento: `docs/cronogramas/PORTFOLIO-REGISTRY-v0.1.md`.

## Bloqueio OpenClaw update
- O Alê determinou: não fazer update do OpenClaw até autorização explícita dele.
- Motivo: a nova versão está dando muito problema.
- Regra: não rodar `gateway update.run` nem update/self-update equivalente sem autorização explícita.

## Pendência — importar cronograma CCSP Casa 7
- Alê pediu registrar a importação do cronograma da CCSP Casa 7 para amanhã ou terça: 27/04/2026 ou 28/04/2026.
- Objetivo: tirar CCSP Casa 7 do status externo/manual e vinculá-la ao Schedule Control/Portfolio Registry.

## Importação CCSP Casa 7 — Schedule Control
- Arquivo recebido no Telegram e identificado como Microsoft Project `.mpp`/CFB real, apesar de metadado Telegram aparecer como TSV.
- Preservado em runtime ignorado pelo Git: `projects/mensura-schedule-control/runtime/inbound/ccsp-casa7/CCSP_CASA_7_Cronograma_Executivo_r0.mpp`.
- Importado para Supabase Mensura Schedule Control como projeto `CCSP_CASA_7`, empresa `MIA`.
- Snapshot criado: `schedule.schedule_versions.version_label = r0_mpp_2026-04-24_telegram_5684`.
- Data de status: 2026-04-24; início 2026-03-23; término 2026-05-22.
- Volume importado: 93 atividades, 14 críticas, avanço médio 53,72%.
- `portfolio.project_registry` atualizado: `CCSP_CASA_7` agora aponta para `schedule.projects`, `source_system=mixed`, `include_in_forecast=true`.
- Lembrete `056557aa-9e29-419b-a0bf-b7e71aeb36c9` removido porque a importação foi concluída.

## PCS/Sienge — cronogramas
- Alê esclareceu: obras do Sienge não têm cronograma disponível.
- Obra do Teatro Suzano está com cronograma em elaboração.
- Atualizado `portfolio.project_registry`: `TEATRO_SUZANO` permanece candidate, fora de forecast e relatório executivo até receber cronograma.

## Portfolio Registry — PCS/Sienge classification + portfolio-report
- Criados comandos `mensura-schedule portfolio-classify-pcs` e `mensura-schedule portfolio-report`.
- PCS/Sienge: 22 registros classificados; nenhum entra em forecast ou relatório executivo porque não há cronograma importável.
- `TEATRO_SUZANO` segue `candidate`, cronograma em elaboração.
- Administrativos marcados: `CUSTOS COM PESSOAL - DIRETORIA`, `ADM PCS`, `ADM MIA ENGENHARIA`, `LOCAÇÃO SÃO CAETANO - VUC PCS`.
- `portfolio-report` validado: 32 registros totais — 8 Mensura, 1 MIA, 23 PCS.
- `portfolio-report --executive-only` validado: `P_G`, `MELICITA_R1`, `CCSP_CASA_7`.

## Mensura Schedule — comando import-mpp productizado
- Criado comando `mensura-schedule import-mpp` para importar arquivo Microsoft Project `.mpp` como snapshot versionado.
- Preview por padrão; gravação apenas com `--execute`.
- Bloqueia reimportação do mesmo `version_label` para evitar duplicidade acidental.
- Atualiza `raw.import_jobs`, `schedule.schedule_versions`, `raw.source_rows`, `schedule.activity_identities`, `schedule.activity_versions` e `portfolio.project_registry`.
- Validado com Casa 7: preview retornou 93 atividades, 14 críticas, início 2026-03-23, término 2026-05-22, avanço médio 53,72%.
- Reimportação do mesmo `version_label` foi bloqueada corretamente.

## Padrão de envio de cronogramas
- Alê propôs reestruturar envio por empresa e por status para respeitar limite de 500 MB.
- Padrão definido: pacotes por empresa/status (`MENSURA_em_andamento.zip`, `MIA_concluidas.zip`, etc.), mantendo cada obra inteira dentro do lote.
- Regra principal: não dividir uma mesma obra em pedaços; se precisar, marcar como parcial.
- Prioridade: em andamento primeiro, concluídas depois para histórico/benchmark.
- Documento criado: `docs/cronogramas/PADRAO-ENVIO-CRONOGRAMAS-v0.1.md`.

## Supabase — intake de pacotes de cronogramas
- Reestruturado no Supabase o padrão de envio por pacotes: criado schema `intake`.
- Migration: `20260427011500_schedule_intake_packages.sql`.
- Tabelas: `intake.schedule_packages`, `intake.schedule_package_items`.
- View: `intake.v_schedule_intake_dashboard`.
- Seed inicial: 6 pacotes planejados (`MENSURA/MIA/PCS` x `em_andamento/concluidas`, lote01).
- CLI criado: `mensura-schedule intake-packages` e `mensura-schedule intake-items`.
- Validação: 6 pacotes listados e 0 itens ainda recebidos.

## Consolidação final retroalimentada — Schedule Control / Portfolio / Intake
- Criado documento consolidado: `docs/cronogramas/SCHEDULE-SYSTEM-CONSOLIDATION-2026-04-26.md`.
- Atualizados os 14 `AGENT-MAP.md` em `/root/2nd-brain/06-agents/*/` com seção sobre Schedule Control, Portfolio Registry e Intake.
- Memória privada da Flávia atualizada em `/root/2nd-brain/06-agents/flavia/memory/2026-04-26.md`.
- Validação CLI antes da consolidação: `portfolio-report --executive-only` retornou `MELICITA_R1`, `P_G`, `CCSP_CASA_7`; `intake-packages` retornou 6 pacotes.
- Backup manual antes dos mapas: `/root/openclaw-backups/manual/agent-maps-before-schedule-system-consolidation-20260426-222056.tar.gz`.

## Portfolio Risk Summary — Relatório de Diretoria
- Alê pediu relatório sucinto de Diretoria com Baseline x término, % planejado x % real, risco executivo e recomendação.
- Criado comando `mensura-schedule portfolio-risk-summary` com saída texto e JSON.
- Validação atual: `P_G`, `MELICITA_R1`, `CCSP_CASA_7` retornam como CRÍTICO.
- Métricas incluídas: baseline_finish, current_finish, delay_days, planned_percent, real_percent, percent_gap, críticas abertas/atrasadas, atividades atrasadas, forecast quando houver e recomendação.
- Observação técnica: `% planejado` inicial é heurístico por atividade; próximo refinamento deve ponderar por duração/custo/WBS.

## Portfolio Risk Summary — avanço por custo e responsáveis
- Alê pediu avanço por custo e principais responsáveis por atrasos no relatório de Diretoria.
- Criada migration `20260427014000_activity_cost_responsibility.sql` com campos em `schedule.activity_versions`: `planned_cost`, `actual_cost`, `remaining_cost`, `cost_weight_percent`, `responsible_name`.
- Backfill aplicado a partir de `raw_payload` (`Custo`, `Custo real`, `Peso da Atividade`, `Responsáveis`).
- `portfolio-risk-summary` atualizado: usa ponderação por custo quando há custo em atividades não-resumo; fallback para média simples quando não há custo.
- Responsáveis ranqueados por impacto de custo, críticas abertas e atividades atrasadas.
- Validação: `P_G` e `MELICITA_R1` usam método `cost_weighted`; `CCSP_CASA_7` usa `simple_average` porque o MPP recebido tem custos zerados e sem responsáveis.
- Sinal importante: `Sem responsável` alto em Melicita/Casa 7 indica falha de cadastro a sanear, não necessariamente responsável executivo real.
